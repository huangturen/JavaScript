其他的都与常见语言的语法一致，需要注意的是相等的判断
1.数据相等的判断

==会主动转换数据类型再比较，结果可能比较诡异，不可信

===不会主动转换数据类型，如果类型不同，则直接返回false


2. NaN与任何数据不相等，包括自己

判断NaN 的方法为isNaN(NaN)

3.区分null和undefined的意义不大

4.数组中元素可以为任何值（包括null）

5.JavaScript中的对象是一组由key-value组成的无序集合

6.如果声明变量时不用var，则该变量为全局变量

7.strict模式下必须使用var来声明变量，否则编译错误

